{
  "name": "nexworth-workspace",
  "version": "1.0.0",
  "scripts": {
    "test": "npx playwright test",
    "test:frontend": "npx playwright test tests/playwright/frontend",
    "test:backend": "npx playwright test tests/playwright/backend",
    "dev:all": "npm run dev:staging",
    "dev:staging": "npx cross-env NODE_ENV=staging concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "dev:all:prod": "npx cross-env NODE_ENV=production concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "dev:prod": "npm run dev:all:prod"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.1",
    "@types/node": "^20.11.0",
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3"
  }
}